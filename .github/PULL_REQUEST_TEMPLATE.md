## What changed

## Why

## Validation

Commands run and their result (for example, `mise run check`, `pnpm --filter @lwc-databento/adapter test`):

## Checklist

- [ ] `mise run check` passes locally
- [ ] Tests added or updated in the same workspace tree as the change
- [ ] No credential-looking value or machine-specific absolute path added to tracked files
