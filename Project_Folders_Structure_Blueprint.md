# Project Folder Structure Blueprint

Canonical layout, placement rules, and layer boundaries live in [ARCHITECTURE.md](./ARCHITECTURE.md).

Update `ARCHITECTURE.md` when a top-level workspace, public entry point, or enforced placement rule changes. `scripts/check-structure.sh` remains the mechanical check.
